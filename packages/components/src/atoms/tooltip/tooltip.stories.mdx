import { Meta, Canvas, Story, ArgsTable } from '@storybook/blocks';
import { html } from 'lit-html';
import { spread } from '@open-wc/lit-helpers';
import './tooltip';
import '../button/button';

<Meta
  title="Atoms/Tooltip"
  argTypes={{
    show: {
      description: 'Show/hide the tooltip.',
      control: { type: 'boolean' },
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: false },
      },
    },
    position: {
      description: 'Position of the tooltip.',
      control: { type: 'select' },
      options: ['top', 'bottom', 'left', 'right'],
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'top' },
      },
    },
    content: {
      description: 'Content of the tooltip.',
      control: { type: 'text' },
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'Tooltip Content' },
      },
    },
  }}
/>

export const Template = ({ content, show, ...args }) => html`<div style="margin: 15rem;">
  <pixel-tooltip ?show=${show} ${spread(args)}>
    <div slot="tooltip-content">${content} Tooltip</div>
    <pixel-button>Button</pixel-button>
  </pixel-tooltip>
</div>`;

# tooltip

## Top Tooltip

<Canvas>
  <Story name="Top" args={{ content: 'Top', position: 'top' }}>
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable story="Top" />

## Bottom Tooltip

<Canvas>
  <Story name="Bottom" args={{ content: 'Bottom', position: 'bottom' }}>
    {Template.bind()}
  </Story>
</Canvas>

## Left Tooltip

<Canvas>
  <Story name="Left" args={{ content: 'Left', position: 'left' }}>
    {Template.bind()}
  </Story>
</Canvas>

## Right Tooltip

<Canvas>
  <Story name="Right" args={{ content: 'Right', position: 'right' }}>
    {Template.bind()}
  </Story>
</Canvas>
