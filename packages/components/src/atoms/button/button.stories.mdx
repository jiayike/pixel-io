import { Meta, Canvas, Story, ArgsTable } from "@storybook/blocks";
import { html } from "lit-html";
import { spread } from "@open-wc/lit-helpers";
import "./button";

<Meta
  title="Atoms/Button"
  args={{
    type: "primary",
    content: "Button",
  }}
  argTypes={{
    type: {
      description: "Style type of button.",
      control: { type: "select" },
      options: ["primary", "secondary"],
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "primary" },
      },
    },
    content: {
      description: "Content of the button.",
      control: { type: "text" },
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "Button" },
      },
    },
    "@onClick": {
      description: "Button clicked event.",
      action: "clicked",
      table: {
        type: { summary: "Event" },
      },
    },
  }}
/>

# Button

## Primary Button

export const Template = ({ content, ...args }) => html`<pixel-button
  ${spread(args)}
  >${content}</pixel-button
>`;

<Canvas>
  <Story name="Primary" args={{ content: "Primary Button" }}>
    {Template.bind()}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Secondary Button

<Canvas>
  <Story
    name="Secondary"
    args={{ type: "secondary", content: "Secondary Button" }}
  >
    {Template.bind()}
  </Story>
</Canvas>
