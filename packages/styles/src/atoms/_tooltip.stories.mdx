import { Meta, Canvas, Story } from "@storybook/blocks";
import { html } from "lit";

<Meta title="Atoms/Tooltip" />

export const Template = ({ content, ...args }) => html`
  <div class="pixel-tooltip">
    <div class="pixel-tooltip__arrow ${args.class}"></div>
    <div class="pixel-tooltip__wrapper">${content}</div>
  </div>
`;

# Tooltip

## Tooltip

<Canvas>
  <Story name="Default">{Template({ content: "Tooltip", class: "pixel-tooltip__arrow--top" })}</Story>
</Canvas>

## Top Tooltip

<Canvas>
  <Story name="Top">
    {html`<div>
      ${Template({
        content: "Top Left",
        class: "pixel-tooltip__arrow--top-left",
      })} ${Template({ content: "Top Middle", class: "pixel-tooltip__arrow--top" })} ${Template({
        content: "Top Right",
        class: "pixel-tooltip__arrow--top-right",
      })}
    </div>`}
  </Story>
</Canvas>

## Bottom Tooltip

<Canvas>
  <Story name="Bottom">
    {html`<div>
      ${Template({
        content: "Bottom Left",
        class: "pixel-tooltip__arrow--bottom-left",
      })} ${Template({ content: "Bottom Middle", class: "pixel-tooltip__arrow--bottom" })} ${Template({
        content: "Bottom Right",
        class: "pixel-tooltip__arrow--bottom-right",
      })}
    </div>`}
  </Story>
</Canvas>

## Left Tooltip

<Canvas>
  <Story name="Left">{Template({ content: "Left", class: "pixel-tooltip__arrow--left" })}</Story>
</Canvas>

## Right Tooltip

<Canvas>
  <Story name="Right">{Template({ content: "Right", class: "pixel-tooltip__arrow--right" })}</Story>
</Canvas>
