import { Meta, Canvas, Story } from "@storybook/blocks";
import { html } from "lit";

<Meta title="Atoms/Button" />

export const Template = ({ content, ...args }) => html`<button class="${args.class}">${content}</button>`;

# Button

## Primary Button

<Canvas>
  <Story name="Primary" args={{ class: "pixel-button pixel-button__primary", content: "Primary Button" }}>
    {Template.bind()}
  </Story>
</Canvas>

## Secondary Button

<Canvas>
  <Story name="Secondary" args={{ class: "pixel-button pixel-button__secondary", content: "Secondary Button" }}>
    {Template.bind()}
  </Story>
</Canvas>
