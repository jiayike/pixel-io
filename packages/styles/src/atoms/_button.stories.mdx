import { Meta, Canvas, Story } from '@storybook/blocks';
import { html } from 'lit';

<Meta title="Atoms/Button" />

export const Template = ({ content, ...args }) =>
  html`<button class="pixel-button pixel-button--${args.type}">${content}</button>`;

# Button

## Primary Button

<Canvas>
  <Story name="Primary" args={{ type: 'primary', content: 'Primary Button' }}>
    {Template.bind()}
  </Story>
</Canvas>

## Secondary Button

<Canvas>
  <Story name="Secondary" args={{ type: 'secondary', content: 'Secondary Button' }}>
    {Template.bind()}
  </Story>
</Canvas>
