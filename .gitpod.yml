image:
  file: .gitpod.Dockerfile
tasks:
  - name: Upgrade npm and pnpm
    command: |
      npm i -g npm;
      npm i -g pnpm;
      echo "Upgrade completed. Exiting...";
      sleep 10;
      exit;
  - name: Setup cypress
    command: |
      pnpx cypress install;
      echo "Cypress setup completed. Exiting...";
      sleep 10;
      exit;
